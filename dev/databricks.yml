# This is a Databricks asset bundle definition for dev.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: dev
  uuid: 8184e6cf-a315-4d3b-b20b-3ba9608cba4b

include:
  - resources/*.yml
  - resources/*/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  warehouse_id:
    description: The warehouse to use
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-facbc479-b4aa.cloud.databricks.com
    variables:
      warehouse_id: eba8f19915cee2dc
      catalog: dbckwork
      schema: aibiwork

  prod:
    mode: production
    workspace:
      host: https://dbc-facbc479-b4aa.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/arti16.gupta@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/arti16.gupta@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      warehouse_id: eba8f19915cee2dc
      catalog: dbckwork
      schema: aibiwork
    permissions:
      - user_name: arti16.gupta@gmail.com
        level: CAN_MANAGE
