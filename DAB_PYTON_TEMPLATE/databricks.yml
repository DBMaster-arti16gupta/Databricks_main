# This is a Databricks asset bundle definition for DAB_PYTON_TEMPLATE.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: DAB_PYTON_TEMPLATE
  uuid: 961816f5-d0e5-4549-b21a-c4fe0e5a7a78

artifacts:
  python_artifact:
    type: whl
    build: uv build --wheel

include:
  - resources/*.yml
  - resources/*/*.yml

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-facbc479-b4aa.cloud.databricks.com

  prod:
    mode: production
    workspace:
      host: https://dbc-facbc479-b4aa.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/arti16.gupta@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/arti16.gupta@gmail.com/.bundle/${bundle.name}/${bundle.target}
    permissions:
      - user_name: arti16.gupta@gmail.com
        level: CAN_MANAGE
